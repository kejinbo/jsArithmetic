// 字典
// 与集合类似，字典也是一种存储谓一值的数据结构，但它是以键值对的形式来存储的。
// es6中有字典，名为Map
const map = new Map();

// 增
map.set("a", 1);
map.set("a", 1);
map.set("b", 2);

// 删
// map.delete("a");
// map.clear();

// 改
map.set("a", 123);
